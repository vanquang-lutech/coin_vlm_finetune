# Minimal dependencies for scripts/serve.py.
#
# Use a fresh serving environment. Do not install the training requirements
# first, because requirements.txt pins a training torch build that can conflict
# with vLLM's compiled CUDA extension.
#
# Recommended install:
#   uv pip install -r requirements-serve.txt
#   uv pip install vllm --torch-backend=auto

numpy>=1.26.4,<2.0.0
Pillow>=10.0.0
opencv-python-headless>=4.10.0.84   # headless: no libGL needed (server/Docker)

omegaconf>=2.3.0
python-dotenv>=1.0.0
pyyaml>=6.0

transformers>=4.57.0

# Pulled transitively by src.data / src.evaluate __init__ (the serving engine
# imports parse_response + CoinEnhancer, which load these packages' __init__).
datasets>=3.0.0
tqdm>=4.66.0

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pyngrok>=7.0.0

# /metrics endpoint (Prometheus exposition). The vLLM engine already publishes
# vllm:* metrics to the default registry; prometheus_client lets us add app-level
# metrics (latency, parse-rate, null-rate) and expose both via one ASGI app.
prometheus-client>=0.20.0
